# LegacyPilot provider-backed baseline prompt v2

You are working on one isolated Java Maven fixture for a governed evaluation. Implement this requirement completely:

{{requirement}}

Inspect the existing production code and make the smallest correct production change. Work only inside the current workspace. Do not modify tests, `pom.xml`, generated files, or files outside the task's declared production scope. Do not use the internet. Run the relevant Maven tests and finish only after checking the implementation.

The evaluation harness will independently enforce allowed files, public assertions, hidden behavior tests, compilation, and tests after your process exits. Reference solutions are not available in this workspace.
